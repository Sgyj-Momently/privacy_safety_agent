# Privacy Safety Agent

Detects photos that should be excluded from public outputs before downstream grouping and writing steps.

## API

- `GET /health`
- `POST /api/v1/privacy-safety`

## Verification

```bash
PYTHON=/path/to/python scripts/verify.sh
```

